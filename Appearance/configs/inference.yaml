# ============================================================================
# PTv3 Gaussian Refiner — Inference Configuration
# ============================================================================
caseName: survival

paths:
  input_dir: results/${caseName}/0-w_00001_Idle
  output_dir: results/${caseName}/3dgs
  attribute_map_path: weights/${caseName}/base_attrmap.npy
  checkpoint: weights/${caseName}/ptv3.pth

processing:
  target_resolution: 1024
  aabb_min: -0.5
  aabb_max: 0.5
  save_input: false
  device: "cuda"
  quiet: false

single_file:
  input: null
  output: null